format_version: 10
pipelines:
  go-cd:
    group: My-Pipelines
    label_template: ${COUNT}
    lock_behavior: none
    display_order: -1
    materials:
      git-2d07c43:
        git: https://github.com/nelson-ragnar/api-go
        shallow_clone: false
        auto_update: true
        branch: main
    stages:
    - build_in_docker:
        fetch_materials: true
        keep_artifacts: false
        clean_workspace: true
        approval:
          type: success
          allow_only_on_success: false
        jobs:
          build-app:
            timeout: 30
            tasks:
            - exec:
                command: go version
                run_if: passed
            - exec:
                command: docker --version
                run_if: passed
            - exec:
                command: docker --version
                run_if: passed
            - exec:
                command: docker --version
                run_if: passed
            - exec:
                command: echo deploy
                run_if: passed
            - exec:
                command: go env
                run_if: passed